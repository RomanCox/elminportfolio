import styled from 'styled-components';

interface TitleStyledPropsType {
    color: string;
}

export const TitleH1Styled = styled.h1<TitleStyledPropsType>`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;

export const TitleH2Styled = styled.h2`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;

export const TitleH3Styled = styled.h3`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;

export const TitleH4Styled = styled.h4`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;

export const TitleH5Styled = styled.h5`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;

export const TitleH6Styled = styled.h6`
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  color: ${({ color }) => color};
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
`;


