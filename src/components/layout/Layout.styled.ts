import styled from 'styled-components';

export const WrapperSidebarStyled = styled.div`
  position: fixed;
  inset: 0;
  z-index: 2;
  display: flex;
  justify-content: flex-start;
  align-items: flex-start;
`;
